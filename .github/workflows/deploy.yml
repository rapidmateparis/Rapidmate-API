name: Deploy to Server

on:
  push:
    branches:
      - UAT

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Server
        run: |
          ssh -i "rapidmate-web-server.pem" ubuntu@ec2-16-171-224-246.eu-north-1.compute.amazonaws.com "sudo su && ./deploy.sh"
